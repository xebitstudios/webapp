// this is a basic model called Contact
var Backbone = require('backbone');

module.exports = ContactModel = Backbone.Model.extend({
    idAttribute: '_id'
    // idAttribute: '_id',
    // urlRoot: 'api/contacts'
});
